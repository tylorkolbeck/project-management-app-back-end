# Migration `20200911021419-add-creator-to-milestone`

This migration has been generated by tylor.kolbeck@gmail.com at 9/10/2020, 7:14:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `todo_db`.`Milestone` ADD COLUMN `creatorId` int  

ALTER TABLE `todo_db`.`Milestone` ADD FOREIGN KEY (`creatorId`) REFERENCES `todo_db`.`User`(`id`) ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200911015652-make-due-date-optional..20200911021419-add-creator-to-milestone
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -17,19 +17,20 @@
   votes       Vote[]
 }
 model User {
-  id               Int       @id @default(autoincrement())
-  createdAt        DateTime  @default(now())
+  id               Int         @id @default(autoincrement())
+  createdAt        DateTime    @default(now())
   name             String
-  email            String    @unique
+  email            String      @unique
   password         String
   links            Link[]
   votes            Vote[]
   projects         Project[] // Projects this User owns
-  projectsAssigned Project[] @relation("projectsAssigned") // Projects this user is a member of
-  tasksAssigned    Task[]    @relation("tasksAssigned")
+  projectsAssigned Project[]   @relation("projectsAssigned") // Projects this user is a member of
+  tasksAssigned    Task[]      @relation("tasksAssigned")
   Task             Task[]
+  Milestone        Milestone[]
 }
 model Project {
   id          Int         @id @default(autoincrement())
@@ -62,8 +63,10 @@
   project     Project?  @relation(fields: [projectId], references: [id])
   projectId   Int?
   dueDate     DateTime?
   tasks       Task[]
+  creator     User?     @relation(fields: [creatorId], references: [id])
+  creatorId   Int?
 }
 model Task {
   id            Int        @id @default(autoincrement())
```


